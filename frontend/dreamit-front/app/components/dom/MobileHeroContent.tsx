"use client";
import React from "react";

interface MobileHeroContentProps {
  locale: 'en' | 'es';
}

export default function MobileHeroContent({ locale }: MobileHeroContentProps) {
  // Component ready for future mobile-specific content
  // Currently empty to avoid covering design elements
  return null;
}